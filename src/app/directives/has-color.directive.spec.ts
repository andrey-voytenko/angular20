import { HasColorDirective } from './has-color.directive';

describe('HasColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HasColorDirective();
    expect(directive).toBeTruthy();
  });
});
